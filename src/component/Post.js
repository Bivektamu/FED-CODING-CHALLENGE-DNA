import React from 'react';
import PropTypes from 'prop-types';

const Post = props => {
  return (
    <div>
      <h2 className='heading'>Post</h2>
    </div>
  );
};

Post.propTypes = {};

export default Post;
